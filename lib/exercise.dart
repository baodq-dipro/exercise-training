double sphericalVolume(double radius) {
  var pi = 3.14;
  return (4 * pi * radius * radius * radius) / 3;
}

double sphereArea(double radius, {double pi = 3.14}) {
  return 4 * pi * radius * radius;
}

double ex2(double x, double y) {
  //  double atan(a);

  // var numerator = log(fabs(x * x - y * y)) / log(5) + atan;

  // var denominator = exp (x) + cos (x + y);

  return 0;
}
